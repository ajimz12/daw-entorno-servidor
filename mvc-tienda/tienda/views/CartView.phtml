<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tu Carrito</title>
</head>

<body>

    <h1>Tu Carrito</h1>

    <?php
    foreach ($orders as $order) {
        echo "<h2>Pedido #" . $order->getOrderId() . "</h2>";
        echo "<p>Estado: " . $order->getStatus() . "</p>";

        foreach ($order->getAllOrderLines() as $orderLine) {

            $product = ProductRepository::getProductById($orderLine->getProductId());
            echo "<p>Producto: " . $product->getName() . "</p>";
        }
    }
    ?>

</body>

</html>