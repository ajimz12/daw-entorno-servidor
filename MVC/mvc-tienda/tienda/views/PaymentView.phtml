<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pago</title>
</head>

<body>
    <h2>Tipo de Pago</h2>
    <?php
    $paymentType = isset($_POST['paymentType']) ? $_POST['paymentType'] : '';
    ?>
    <form method="POST" action="index.php?c=cart" enctype="multipart/form-data">
        <label for="paymentType">Elige el método de pago:</label>
        <select id="paymentType" name="paymentType" onchange="this.form.submit()">
            <option value="transfer" <?php if ($paymentType == 'transfer') echo 'selected'; ?>>Transferencia</option>
            <option value="card" <?php if ($paymentType == 'card') echo 'selected'; ?>>Tarjeta</option>
        </select>

        <?php if ($paymentType == 'card'): ?>
            <div>
                <h3>Detalles de la Tarjeta</h3>
                <label for="cardNumber">Número de Tarjeta:</label>
                <input type="text" id="cardNumber" name="cardNumber" maxlength="16" placeholder="1234 5678 9012 3456">
                <br>
                <label for="expiryDate">Fecha de Vencimiento:</label>
                <input type="month" id="expiryDate" name="expiryDate">
                <br>
                <label for="cvv">CVV:</label>
                <input type="text" id="cvv" name="cvv" maxlength="3" placeholder="123">
            </div>
        <?php endif; ?>

        <br>
        <button type="submit" name="payOrder">Confirmar Pago</button>
    </form>
</body>

</html>